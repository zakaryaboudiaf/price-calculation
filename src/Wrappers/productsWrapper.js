import styled from "styled-components";

const Wrapper = styled.div`

    flex-grow: 1;
    margin: 0.5rem;
    padding: 0.5rem;
    min-width: 300px;
    max-width: 550px;
    overflow-x: auto;

    .header-title{
        color: rgb(64, 64, 64);
    }

`

export default Wrapper